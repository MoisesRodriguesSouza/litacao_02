// Placeholder: lib/features/process_management/screens/new_process_page.dart
import 'package:flutter/material.dart';

class NewProcessPage extends StatelessWidget {
  const NewProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Novo Processo')),
      body: const Center(
        child: Text('Tela de Cadastro de Novo Processo (a ser implementada)'),
      ),
    );
  }
}
